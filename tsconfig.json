{
  "compilerOptions": {
    "target": "es2015", // Target ES2015 (ES6) for better performance in modern environments
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ], // DOM and ESNext libraries for modern features
    "allowJs": true, // Allow JavaScript files to be included in the project
    "skipLibCheck": true, // Skip type checking of declaration files in node_modules for faster compilation
    "strict": true, // Enable all strict type-checking options (can be turned to "false" for relaxed checking)
    "noEmit": true, // Prevent TypeScript from emitting compiled files (typically used with Next.js)
    "esModuleInterop": true, // Enable compatibility between CommonJS and ESModules
    "module": "esnext", // Use ESNext module resolution, typically for bundlers like Webpack
    "moduleResolution": "bundler", // Resolve modules according to bundler (Webpack/Vite) behavior
    "resolveJsonModule": true, // Allow JSON imports
    "isolatedModules": true, // Ensure each file is treated as an isolated module (required by Next.js)
    "jsx": "preserve", // Keep JSX in the output (used in React projects)
    "incremental": true, // Enable incremental compilation for faster rebuilds
    "paths": {
      "@/*": [
        "./src/*"
      ] // Set up module aliasing for shorter import paths
    },
    "typeRoots": [
      "./node_modules/@types",
      "./src/types"
    ] // Specify where to find type declaration files
    ,
    "plugins": [
      {
        "name": "next"
      }
    ]
  },
  "include": [
    "next-env.d.ts", // Include Next.js environment types
    "**/*.ts", // Include all TypeScript files
    "**/*.tsx", // Include all TypeScript JSX files (React components)
    "**/*.js", // Include all JavaScript files
    "**/*.jsx", // Include all JavaScript JSX files (React components)
    ".next/types/**/*.ts" // Include Next.js-generated types
  ],
  "exclude": [
    "node_modules" // Exclude node_modules to avoid unnecessary type-checking of third-party libraries
  ]
}
